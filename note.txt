npx prisma migrate dev --name <migration_name>
npx prisma db seed
dùng middleware để xác thực input, có thể dùng lại middleware cho nhiều api

- truyền tham số orderBy vào controller

- Làm trang chi tiết bài viết